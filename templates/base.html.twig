<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500&display=swap" rel="stylesheet">
    <script src="https://kit.fontawesome.com/3cd616181c.js" crossorigin="anonymous"></script>
    <link rel="icon"
          href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
    <link rel="stylesheet" href="{{ asset('css/app.css') }}">
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-size: 1rem;
            font-family: 'Roboto', sans-serif;
            color: var(--secondary-dark);
        }

        :root {
            --primary-light: #FBFBFB;
            --secondary-light: #D7DADF;
            --primary-dark: #171717;
            --secondary-dark: #333539;
            --thirdy-dark: #4D585F;
            --fourth-darl: #677E84;
        }

        .container {
            max-width: 1320px;
            width: 100%;
            margin-right: auto;
            margin-left: auto;
        }

        .mt {
            margin-top: 1rem;
        }

        .mb {
            margin-bottom: 1rem;
        }

        .title {
            font-weight: 500;
            font-size: 1.8rem;
            margin-bottom: 1rem;
        }

        .btn {
            border: none;
            cursor: pointer;
            padding: 5px 10px;
            border-radius: 5px;
            display: inline-block;
            text-decoration: none;
            transition: all .3s linear;
        }

        .btn i {
            margin: 0 .1rem;
            font-size: .8rem;
            color: var(--primary-light);
        }

        .btn.btn-link {
            color: var(--primary-light);
            background-color: var(--primary-dark);
        }

        .btn.btn-link:hover {
            background-color: var(--secondary-dark);
        }

        .btn.btn-input {
            color: var(--primary-light);
            background-color: var(--thirdy-dark);
        }

        .btn.btn-input:hover {
            background-color: var(--fourth-darl);
        }

        .form-control label {
            display: block;
            margin-bottom: 5px;
        }

        .form-control input {
            width: 100%;
            padding: 5px;
            border-radius: 5px;
            border: 1px solid var(--primary-dark);
        }

        .form-control input:focus-visible {
            outline: none;
            border: 1px solid var(--thirdy-dark);
        }
    </style>

    {% block stylesheets %}
    {% endblock %}

    {% block javascripts %}
    {% endblock %}
</head>
<body>
<div class="container mx-auto">
    {% block body %}{% endblock %}
</div>
</body>
</html>
